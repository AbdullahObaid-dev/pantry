import type { NextConfig } from 'next';

// No custom config needed yet — Tailwind/theme config lands with the first
// real UI stage (see chat response / CONTRACT.md session log for Stage 00a).
const nextConfig: NextConfig = {};

export default nextConfig;
